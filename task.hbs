<h2>{{task.text}}</h2>

<div class="task-info">
    {{#if task.completed}}
        <p>{{task.text}} is completed.</p>
    {{else}}
        <p>{{task.text}} is not yest completed</p>
    {{/if}}
</div>